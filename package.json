{
  "name": "dsca-mandatory-training-assessment",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "vercel-build": "cd frontend && npm install && npm run build"
  },
  "engines": {
    "node": "20.x"
  }
}
